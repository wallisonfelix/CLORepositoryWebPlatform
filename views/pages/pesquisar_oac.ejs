<!DOCTYPE html>
<html lang="pt-BR">
<head>
	<% include ../partials/head %>
</head>

<body style="background-color: #fffffa;">
	<% include ../partials/page_header %>

	<div class="container">
	  <% include ../partials/header %>

	  <div class="row">
	    <div id="menu" class="col-sm-3">
	    	<% include ../partials/menu %>
	    </div>	    	

	    <!-- Page Content -->
	    <div class="col-sm-9">
	    	<ol class="breadcrumb">
			  <li>Home</a></li>
			  <li class="active">Pesquisar OAC</li>
			</ol>

			<form action="/pesquisarOAC" role="form" class="form-horizontal" accept-charset="UTF-8">
				<div class="form-group">
					<label for="title" class="control-label col-sm-2">Título do OAC:</label>
					<div class="col-sm-10">
						<input id="title" name="title" type="text" value='<%= title %>' class="form-control"/>
						<span class="help-block">Informe o título do OAC que deseja encontrar</span>
					</div>
				</div>
				<div class="form-group">
					<div class="col-sm-offset-2 col-sm-10">
						<input type="submit" name="btnPesquisar" class="btn btn-info" value="Pesquisar"/>
					</div>	
				</div>
			</form>

			<div id="results">
				<h4>Resultados:</h4>
				<table class="table table-striped table-responsive">
				    <thead>
				      	<tr>
				        	<th class="text-center">Título</th>
				        	<th class="text-center">Detalhes</th>
				        	<th class="text-center">Arquivos Executáveis</th>
				        	<th class="text-center">Versões Customizadas</th>
				      	</tr>
				    </thead>
				    <tbody>
				    <% if(data) {
						for(var i = 0; i < data.length; i++) { %>
				      		<tr>
					        	<td>%= data[i].title %></td>
					        	<td class="text-center">
					        		<a href="#">
						        		<span class="glyphicon glyphicon-search" />
						        	</a>
						        </td>
						        <td class="text-center">
						        	<% 	for(var j = 0; j < data[i].files.length; j++) { 
											for(var k = 0; k < data[i].files[j].info.length; k++) { %>
								        		<tr>
									        		<a href="localhost/OAC?id=<%= data[i].files[j]._id%>&filePath=<%= data[i].files[j].info[k].path%>">
									        			<%= data[i].files[j].info[k].ext %>
									        		</a>
									        	</tr>
									    <%	}
									    } %>	
						        </td>
						        <td class="text-center">
						        	<% 	for(var j = 0; j < data[i].files.length; j++) { 
											for(var k = 0; k < data[i].files[j].info.length; k++) { %>
								        		<tr>
									        		<a href="#">
									        			<span class="glyphicon glyphicon-folder-open" />
									        			<!-- Analisar a apresentação da quantidade de versões customizadas -->
				        								<span class="badge"><%= data[i].length %></span>
									        		</a>
									        	</tr>
									    <%	}
									    } %>	
						        </td>
			      			</tr>
				      	<%	}
						} %>
				    </tbody>
				</table>

				<% if(!data) { %>
					<div class="alert alert-warning fade in">
						Nenhum OAC retornado
					</div>
				<% } %>	

			</div>
	    </div>

	</div>

	<% include ../partials/page_footer %>
</body>

</html>