- Implementar a autenticação com passport
- Criar página de cadastro de usuário
- Criar página de login
- Adicionar um fluxo de administrador para validar o papel solicitado pelo usuário
- Adicionar as validações para cada página

//Para verificar se um usuário possui uma permissão (operação)

//User
roles = {};
function carregarPapel(roleCode, role) {
	map[roleCode] = role; 
}
function hasRole(roleCode) {
	return (roleCode in roles); 
}
//Com Sequelize, apenas será necessário chamar o get da lista de Roles passando um parâmetro
user.getRoles({ where: 'roleCode = "abc..."', attributes: ['roleCode'] }).then(function(role) {
  
})


--- 
Remover papel
Verificação do encontro nos casos de edição
Informação de campo obrigatório
Replicar tudo para operações
