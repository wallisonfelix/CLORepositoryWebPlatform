- Implementar a autenticação com passport
- Criar página de login 
(pede usuário e senha 
linkar para o cadastro e para a recuperação de senha 
redirecionará o usuário para o index informando que foi autenticado com sucesso
caso não tenha sido autenticado, informará usuário e/ou senha inválidos na própria tela de login)
- Criar página de recuperação de senha 
(pede o email cadastrado 
envia um email para o usuário para ele fazer a recuperação
o email, nos moldes do email de validação de email
ao clicar no link, irá para uma tela, caso esteja o link correto, nela ele irá alterar a senha
caso o link esteja errado, redireciona para a área de trabalho informando que o link está errado)
- Adicionar as validações de acesso para cada página (tratadores de request no main.js - sempre ter um if no início - Acesso negado...)

//Para verificar se um usuário possui uma permissão (operação)

//User
roles = {};
function carregarPapel(roleCode, role) {
	map[roleCode] = role; 
}
function hasRole(roleCode) {
	return (roleCode in roles); 
}
//Com Sequelize, apenas será necessário chamar o get da lista de Roles passando um parâmetro
user.getRoles({ where: 'roleCode = "abc..."', attributes: ['roleCode'] }).then(function(role) {
  
})


--- 

Falta implementar
- Validação de email
- Carregar apenas emails validados na pesquisa de Usuário
- Pesquisa de usuário para a validação (na pesquisa aparece se foi ou não validado - Não um X em vermelho) - Incluir opção de validar
- Edição de usuário validando-o (email informando o usuário no final - falar dos dados dele).
- Troca de senha